<? foreach($this->fields as $key => $data): ?>
<? $isTextarea = $data['type'] === 'textarea'; ?>
<? if ($isTextarea): ?>
-----
<?=$key?>:
<?=$data['value']?>

-----
<? else: ?>
<?= "$key: " . $data['value'] . PHP_EOL ?>
<? endif ?>
<? endforeach ?>