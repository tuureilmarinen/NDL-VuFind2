<? $cnt = 0; ?>
<ul class="article-links">
<? foreach ($this->items as $item): ?>
  <?
    $link = $this->baseUrl . $cnt;
    if ($this->feedUrl) {
       $link .= '?feedUrl=' . $this->escapeHtmlAttr($this->feedUrl);
    }
  ?>
  <li><?= $cnt != $num ? ('<a href="' . $link . '">' . $item . '</a>') : $item ?></li>
  <? $cnt++;?>
<?endforeach;?>
</ul>
